import english from './languages/en'
import norwegian from './languages/no'

export default {
    locale: 'en',
    fallbackLocale: 'en',
    messages: {
        en: english,
        no: norwegian,
    }
}